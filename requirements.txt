# Install torch for your CUDA build first, e.g. for CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# torch >= 2.4 is required for the device-first torch.amp API
# (torch.amp.GradScaler("cuda", ...) / torch.amp.autocast("cuda", ...)).
torch>=2.4
numpy>=1.24
pandas>=2.0
scipy>=1.10
pillow>=10.0
