# Install torch built for your CUDA version first, following the official selector
# at pytorch.org (the CPU wheel will not run this solution).
#
# torch >= 2.4 is required for the device-first torch.amp API
# (torch.amp.GradScaler("cuda", ...) / torch.amp.autocast("cuda", ...)).
torch>=2.4
numpy>=1.24
pandas>=2.0
scipy>=1.10
pillow>=10.0
